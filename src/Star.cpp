/*#include "Star.h"


Star::Star(float radius, float mass, float orbitAngle, float distance, float orbitSpeed, float rotationSpeed, glm::vec3 color):
        radius(radius), mass(mass), orbitAngle(orbitAngle),distance(distance),
        orbitSpeed(orbitSpeed), rotationSpeed(rotationSpeed), color(color),
        model(glm::mat4(1.0f)) {{


            unsigned int VAO, VBO;

            glGenBuffers(1, &VAO);
            glGenVertexArrays(1, &VAO);

            glBindVertexArray(VAO);
            glBindBuffer(GL_VERTEX_ARRAY,VBO);
            glBufferData(GL_VERTEX_ARRAY, GL_DYNAMIC_DRAW);



}*/