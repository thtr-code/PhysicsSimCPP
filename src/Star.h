/*#pragma once
#include "Shader.h"


class Star{
public:
    float mass, radius, orbitspeed, orbitAngle, orbitSpeed, rotationSpeed, distance;

    Star(float radius, float mass, float orbitAngle, float distance, float orbitSpeed, float rotationSpeed, glm::vec3 color);
    int rings, segments;

private:
    glm::vec3 color;
    glm::vec4 model;

    void generateStar();
};*/